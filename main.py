import parser_db as pd
import parser_http as ph
import re


def main():
    # pd.add_db()
    det = re.sub(r'^.*([A-Za-z0-9])\w+\s-\s', '', d[2])
    det = re.sub(r'^\([^)]*\)', '', det)


if __name__ == '__main__':
    main()
